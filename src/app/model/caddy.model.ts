export class Caddy {

    ref: number;
    name: string;
    sum: number;
    qty: number

    constructor(ref: number, name: string, sum: number, qty: number) {
        this.ref = ref
        this.name = name
        this.sum = sum
        this.qty = qty

    }
}